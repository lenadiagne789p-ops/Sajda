/// Centralise les clés/API sensibles. Renseignez les valeurs via Dreamflow
/// (ne commitez pas de clés réelles).
class AppSecrets {
  AppSecrets._();

  /// Clé API Google Places utilisée pour enrichir les fiches mosquées.
  /// Définissez la valeur depuis l’éditeur Dreamflow avant publication.
  static const String googlePlacesApiKey = '';
}